# MNIST Digit Recognition

This project uses the MNIST dataset and a Neural Network to recognize handwritten digits (0–9).

## Technologies Used
- Python
- TensorFlow
- NumPy

## How to Run

Install dependencies:

```bash
pip install -r requirements.txt
